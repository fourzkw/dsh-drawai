# 本机自测用的 @deepseek-ai/* junction，机器相关，不要提交
node_modules/

# 本机 npm 缓存在 ~/.npm 只读时用的本地目录（见 README 的「开发」一节）
.npm-cache/

# 编辑器 / 系统
.vscode/
.idea/
Thumbs.db
desktop.ini
*.log
# 画布文件：仓库里只留 demo.drawio（示例图，画布默认读它）
# 其余 *.drawio 都是各人工作区里的图，不进仓库
*.drawio
!demo.drawio